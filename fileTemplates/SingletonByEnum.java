#if (${PACKAGE_NAME} && ${PACKAGE_NAME} != "")package ${PACKAGE_NAME};#end
/**
 * ${Description}
 *
 * Created by ${USER} 
 * on ${YEAR}-${MONTH}-${DAY} ${HOUR}:${MINUTE}.
 * @author ${USER}
 */
public enum ${NAME} {
    // instance
    INSTANCE;
    
}